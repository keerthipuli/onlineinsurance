// Redirect Function
function redirectTo(page) {
    alert(`Redirecting to ${page} management page...`);
    // You can add actual redirection logic here
    console.log(`Redirected to ${page}`);
}
